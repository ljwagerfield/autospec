package spike.runtime

class EndpointRequestExecutor {
  def apply(history: List[EndpointRequestResponse], request: EndpointRequest): EndpointResponse = {
    // Execute the request against the API.
  }
}
